# 本项目包管理器固定 pnpm（见 MASTER-PLAN T5.1：仓库只保留 pnpm-lock.yaml）。
# registry 指向国内镜像：本机走加速器时官方源会间歇性 reset，镜像更稳；
# 想换回官方源：把这行删掉或改成 https://registry.npmjs.org/
registry=https://registry.npmmirror.com

# 说明：pnpm 的内容寻址仓库已在本机配置为 D:\.pnpm-store（`pnpm store path` 可验证），
# 不会把依赖缓存写进 C 盘；node_modules 也只在 frontend/ 下（D 盘）。
